require 'spec_helper'

describe International do
  it 'has a version number' do
    expect(International::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
